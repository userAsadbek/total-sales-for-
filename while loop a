include <iostream>
using namespace std;
int main() {
    int x=0,y=0,z;
    cout<<"How many numbers do you want to enter?";
    cin>>z;
    while (x<z) {
        int a;
        cout<<"Enter a number:";
        cin>>a;
        y+=a;
        x++;
    }
}
